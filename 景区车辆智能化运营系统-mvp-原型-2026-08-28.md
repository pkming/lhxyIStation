# 景区车辆智能化运营系统 — MVP 原型

基于上传的需求文档，做一套可点击演示的原型：4 个界面 + 一套统一设计系统，全部使用模拟数据（含定时刷新，看起来像实时）。共 80 台车：10 台大巴 + 70 台摆渡车。

## 页面结构

```text
/                 调度总览（后台工作台，默认首页）
/stations         站台客流采集看板
/terminal         车载终端模拟界面（司机端）
/rider            游客扫码查询页（移动端布局）
```

## 1. 后台调度工作台 `/`
- 顶部 KPI：在线车辆 / 总车辆、当前候车总人数、平均候车时长、今日发班数
- 主区左侧：景区示意地图（SVG 手绘线路 + 站点 + 车辆图标沿线路移动，摆渡车与大巴不同配色）
- 主区右侧：实时派单流水（客流触发 → 就近派车 → 已推送 → 司机同意/拒绝）
- 下部标签页：摆渡车列表（就近自动派班）／大巴车列表（时刻表排班，含 4-5 个中途站点）
- 车辆行：车牌、司机、状态（营运/待命/离线）、当前站点、载客、3 路视频在线状态
- 手动派单按钮 → 生成一条派单指令进入流水

## 2. 站台客流看板 `/stations`
- 各站台卡片：在场人数、趋势迷你图、拥挤度（低/中/高）、已派车辆数
- 运力分析条：客流 vs 可用车辆，超阈值站台高亮告警
- 30 分钟客流曲线图（Recharts）

## 3. 车载终端模拟 `/terminal`
- 模拟车载屏：车牌、线路、时间、卫星定位与时钟同步状态
- 派单弹层：来单提示 + 大号「同意 / 拒绝」实体按键样式，倒计时自动过期
- 报站区：当前站/下一站、自动/手动报站切换、语音播报模拟
- 3 路摄像头缩略画面占位（前车厢/后车厢/车门）与录像状态

## 4. 游客扫码查询 `/rider`
- 手机宽度布局，顶部当前站台（扫码进入的站点）
- 最近车辆列表：车辆编号、距离、预计到站分钟数、剩余座位
- 线路示意条 + 下一班大巴时刻表
- 自动刷新倒计时

## 设计方向
- 深色调度控制台风格（后台/终端），游客端为浅色轻量；不用紫蓝渐变套路
- 主色：山林青绿 + 琥珀告警色；等宽数字字体做数据显示
- 全部颜色写入 `src/styles.css` 语义 token，中文界面文案

## 技术要点
- 纯前端模拟数据层：`src/lib/mock/`（车辆、站台、派单、时刻表）+ `useSimulation` hook，setInterval 推进车辆位置与客流波动
- 车辆运动用参数化线路点插值，SVG 渲染，无地图库依赖
- 图表用已有 recharts；组件用 shadcn（Card/Tabs/Badge/Button/Dialog）
- 每个路由独立 `head()` 元数据；共享侧边导航放 `__root.tsx`
- 不接数据库；后续需要真实数据可再启用 Lovable Cloud
